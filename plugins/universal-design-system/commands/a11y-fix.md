---
description: Diagnose and fix accessibility test failures (144 axe-core tests). Traces contrast failures to specific tokens, adjusts values, cascades fixes across all docs pages, and re-runs tests.
---

Read the agent instructions from `.claude/agents/a11y-remediator.md` and execute the full remediation protocol. Spawn a general-purpose agent for autonomous execution, or run directly if invoked as a command.
