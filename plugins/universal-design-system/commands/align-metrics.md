---
description: Compute actual project metrics from source files and update all references in CLAUDE.md, README.md, and docs to match. Prevents documentation number drift.
---

Read the agent instructions from `.claude/agents/metrics-aligner.md` and execute the full metrics alignment protocol. Spawn a general-purpose agent for autonomous execution, or run directly if invoked as a command.
