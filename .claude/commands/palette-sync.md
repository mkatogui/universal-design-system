---
description: Sync palette token values from design-tokens.json into all 8 docs pages (light + dark mode CSS blocks). Handles expanded vs minified CSS formatting per page.
---

Read the agent instructions from `.claude/agents/palette-sync.md` and execute the full palette sync protocol. Spawn a general-purpose agent for autonomous execution, or run directly if invoked as a command.
